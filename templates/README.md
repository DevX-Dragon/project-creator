# Template Library

This folder contains starter templates that can be downloaded by Project Creator.

Current templates:

- `basic-web`
- `npm-package`
- `python-pypi`

Each template is meant to be copied into a fresh project folder and then customized.
