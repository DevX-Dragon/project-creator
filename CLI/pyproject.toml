[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prj-creator"
version = "1.0.1"
description = "Industrial project initializer for hardware and software."
readme = "README.md"
authors = [
  { name="DevX-Dragon", email="devx-dragon@users.noreply.github.com" },
]
license = { text="MIT" }
requires-python = ">=3.7"

[project.scripts]
project-creator = "main:run_wizard"